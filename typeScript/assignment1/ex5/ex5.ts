let famous_person : string = "Prophet Muhammas (SAW)";
let quote1 : string = "No two things have been combined better than knowledge and patience";
let message: string = `"${famous_person} once said, "${quote1}"`;
console.log(message);
